<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Diary Books</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Diary Books</a>
            <% if (locals.user) { %>
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="/auth/logout">Logout</a>
                </div>
            <% } %>
        </div>
    </nav>

    <main class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-body text-center py-5">
                        <div class="mb-4">
                            <% if (locals.status === 404) { %>
                                <i class="fas fa-search text-warning" style="font-size: 4rem;"></i>
                            <% } else if (locals.status >= 500) { %>
                                <i class="fas fa-exclamation-triangle text-danger" style="font-size: 4rem;"></i>
                            <% } else if (locals.status === 401) { %>
                                <i class="fas fa-lock text-warning" style="font-size: 4rem;"></i>
                            <% } else { %>
                                <i class="fas fa-exclamation-circle text-warning" style="font-size: 4rem;"></i>
                            <% } %>
                        </div>
                        
                        <h2 class="card-title mb-3">
                            <% if (locals.status) { %>
                                <%= status %> - <%= title %>
                            <% } else { %>
                                <%= title %>
                            <% } %>
                        </h2>
                        
                        <p class="card-text text-muted mb-4"><%= message %></p>
                        
                        <% if (error && error.stack && process.env.NODE_ENV === 'development') { %>
                            <details class="mt-4">
                                <summary class="btn btn-sm btn-outline-secondary mb-3">Show Debug Info</summary>
                                <pre class="text-start p-3 bg-light" style="max-height: 300px; overflow-y: auto;"><%= error.stack %></pre>
                            </details>
                        <% } %>
                        
                        <div class="d-flex gap-2 justify-content-center mt-4">
                            <% if (locals.status === 401) { %>
                                <a href="/auth/login" class="btn btn-primary">
                                    <i class="fas fa-sign-in-alt me-2"></i>Go to Login
                                </a>
                            <% } else { %>
                                <button onclick="history.back()" class="btn btn-outline-secondary">
                                    <i class="fas fa-arrow-left me-2"></i>Go Back
                                </button>
                                <a href="/" class="btn btn-primary">
                                    <i class="fas fa-home me-2"></i>Go Home
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>